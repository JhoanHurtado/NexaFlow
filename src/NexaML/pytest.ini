[pytest]
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Markers
markers =
    unit: tests that run without external dependencies (DB, AWS, Prophet)
    integration: tests that require pandas/prophet/asyncpg installed
